buildscript {
  repositories {
      jcenter()
  }

  dependencies {
    classpath 'org.ajoberstar:gradle-git-publish:0.2.1'
  }
}

apply plugin: 'org.ajoberstar.git-publish'

gitPublish {
    // where to publish to (repo must exist)
    repoUri = 'git@github.com:mkristiansen/example.git'
    // branch will be created if it doesn't exist
    branch = 'gh-pages'

    // generally, you don't need to touch this
    repoDir = file("$buildDir/gh-pages")

    // what to publish, this is a standard CopySpec
    contents {
        from './_book'
    }

    // message used when committing changes
    //commitMessage = 'Publishing a new page' // defaults to 'Generated by gradle-git-publish'
}
